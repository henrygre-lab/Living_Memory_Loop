# Fonts

Add the Inter font files here and include them in the Xcode target:

- `Inter-Regular.ttf`
- `Inter-Medium.ttf`
- `Inter-SemiBold.ttf`
- `Inter-Bold.ttf`

The `Info.plist` template already includes these names under `UIAppFonts`.
